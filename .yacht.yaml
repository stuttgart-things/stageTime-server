---
revisionrun:
  - build-yas-image:
      stage: 0
      pipeline: build-kaniko-image
      params: dockerfile=./Dockerfile; git-revision={{ .gitRevision }}, gitRepoUrl={{ .gitRepoUrl }}
  - build-yas-chart:
      stage: 1
      pipeline: build-helm-chart
      params: ""
